// import React, { createContext } from 'react';

// interface ProviderProps {
//   children: React.ReactNode;
// }

// interface CacheManager {
//   loader: any;
//   cache: any;
// }

// const ResourceContext = createContext({});
// const resourceManager: CacheManager = {};

// const ResourceProvider: React.FC<ProviderProps> = ({ children }) => <ResourceContext.Provider value={cacheManager}>{children}</ResourceContext.Provider>;

// export default ResourceProvider;
